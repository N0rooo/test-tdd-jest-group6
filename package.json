{
  "name": "deploiement-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run start",
    "test": "jest --watchAll --coverage --verbose --silent --runInBand",
    "test:cov": "jest --coverage"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "faker-br": "^0.1.0"
  }
}
